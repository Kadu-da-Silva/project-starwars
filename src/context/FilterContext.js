import { createContext } from 'react';

const FilterContext = createContext();

export default FilterContext;

// Esse contexto servirá como um container para compartilhar e acessar o estado global e as funções relacionadas aos filtros em toda a aplicação.
